<h1>Listing books</h1>

<table class="books">
  <tr>
    <th>Thumbnail</th>
    <th>Title/Author</th>
    <th>Command</th>
    <th>Status</th>
  </tr>

<% @books.each do |book| %>
  <tr>
    <td class="thum"><center>
    <%= image_tag(book.image.path) unless (book.image == nil) %>
    </center></td>

    <td class="title">
      <%= book.title %><br />
      <%= book.author %><br />
      <%= book.publisher %><br />
    </td>

    <td class="command">
        <%= link_to 'Amazon', book.amazon_url %><br />
        <%= link_to 'Show', book %><br />
        <%= link_to 'Edit', edit_book_path(book) %><br />
        <%= link_to 'Destroy', book, confirm: 'Are you sure?',
          method: :delete %><br/ >
<!--          <%= form_tag(:controller => "subscription_requests", :action => :new_from_book_list, :method => "post") do  %>
          <%= hidden_field_tag 'book_id', book.id %>
          <%= submit_tag "Rent" %>-->
        <%= link_to 'Rent', :controller => "subscription_requests", :action => "new_from_book_list", :book => book %><br/>
        <%= link_to 'Wirte Review', :controller => "reviews", :action => "new_from_book_list", :book => book %><br/>
        <%= link_to 'List of Subscription Requests', :controller => "books", :action => "list_subscription_requests", :book => book %>
        <% end %>
    </td>
    <td>
      <% if book.status == 0 %>
        <%= "あるよ" %>
      <% elsif book.status == 1 %>
        <%= "貸し出し中" %>
      <% end %>
    </td>
  </tr>
<% end %>

<br />
<div id="side">
  <%= link_to 'New Book', new_book_path %><br />
  <%= link_to 'New Book from ISBN', new_from_isbn_path %><br/ >
  <%= link_to 'New Book from Plural ISBN', new_from_plural_isbn_path %><br />
</div>
</table>

<%= will_paginate(@books) %>
