<%= form_for(@subscription_request) do |f| %>
  <% if @subscription_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subscription_request.errors.count, "error") %> prohibited this subscription_request from being saved:</h2>

      <ul>
      <% @subscription_request.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :book %><br />
    <%= f.hidden_field :book_id, :value => @subscription_request.book.id %>
    <%= @subscription_request.book.title %>
  </div>
  <div class="field">
    <%= f.label :user %><br />
    <%= f.hidden_field :user_id, :value => @subscription_request.user.id %>
    <%= @subscription_request.user.name %>    
  </div>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, :rows => 3 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
